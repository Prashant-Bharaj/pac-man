[project]
name = "pac-man"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pygame>=2.5.0",
]

[dependency-groups]
dev = [
    "flake8>=7.0.0",
    "mypy>=1.8.0",
    "pytest>=8.0.0",
]
